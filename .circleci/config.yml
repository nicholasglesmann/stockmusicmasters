 version: 2.1

 orbs:
  win: circleci/windows@2.2.0

 jobs:
   build:
     executor: win/default     
    
     steps:
       - checkout
       - run:
           name: "test"
           command: |
             echo 'export MSDEPLOY_SERVICE_URL=$MSDeployServiceURL' >> $BASH_ENV
             source $BASH_ENV
             sed -i s/MSDEPLOY_SERVICE_URL/${MSDEPLOY_SERVICE_URL}/ webdeploy.pubxml
             echo "$(cat webdeploy.pubxml)"
           shell: bash.exe
