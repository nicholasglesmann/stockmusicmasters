 version: 2.1

 orbs:
  win: circleci/windows@2.2.0

 jobs:
   build:
     executor: win/default     
    
     steps:
       - checkout
       - run:
           name: "test"
           command: |
             echo 'export WEBDEPLOY=$webdeploy' >> $BASH_ENV
             source $BASH_ENV
             echo $WEBDEPLOY
           shell: bash.exe
